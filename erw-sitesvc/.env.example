# ERW City Viability Analysis Platform - Environment Configuration

# Database Configuration (Required)
DB_URL=jdbc:postgresql://localhost:5432/erwdb
DB_USER=your_database_username
DB_PASS=your_secure_database_password

# Java Configuration (Optional - only needed for non-standard installations)
JAVA_HOME=/path/to/java-21

# Application Configuration (Optional)
SERVER_PORT=8080

# Docker Compose Database Credentials (Development Only)
POSTGRES_DB=erwdb
POSTGRES_USER=erw_dev
POSTGRES_PASSWORD=secure_dev_password_change_in_production

# Security Notes:
# 1. Never commit actual credentials to version control
# 2. Copy this file to .env and update with your actual values
# 3. Ensure .env is listed in .gitignore
# 4. Use strong passwords for production deployments
# 5. Consider using Docker secrets or Kubernetes secrets for production