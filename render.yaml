services:
  - type: web
    name: erw-city-viability
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: DB_URL
        fromDatabase:
          name: erw-postgres
          property: connectionString
      - key: DATABASE_URL
        fromDatabase:
          name: erw-postgres
          property: connectionString

databases:
  - name: erw-postgres
    plan: free